<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!--
      E-5. Client answer Q-17 replaced the role model with two roles, Maker and Checker,
      and changeset 040 seeded them. But every existing account still holds a legacy role,
      and SecurityConfig now gates /api/procurement/** on the new ones - so the module is
      unreachable for everyone who actually has a login.

      This grants the new procurement permissions to the legacy roles rather than
      reassigning users. Reassigning would be presumptuous: which of DD1-DD4 should be
      able to approve a stage is BPDB's decision, not ours, and a migration that silently
      promoted four levels of Deputy Director to Checker could hand approval rights to
      people who are not meant to have them.

      The mapping applied here is deliberately conservative:

        ADMIN            already has everything
        DD1 .. DD4       capture only - they upload documents today, so they become Makers
        OFFICER          capture only, for the same reason

      Nobody is granted approval rights by migration. Checkers are appointed by an
      administrator assigning the CHECKER role to named people, which is a decision that
      should be made deliberately and be visible in the audit trail.
    -->

    <changeSet id="042-001" author="dms">
        <comment>Q-17/E-5: let legacy roles capture procurement data, so existing logins still work</comment>
        <sql>
INSERT INTO role_permissions (role_id, permission_id)
SELECT r.id, p.id
  FROM roles r, permissions p
 WHERE r.name IN ('OFFICER', 'DD1', 'DD2', 'DD3', 'DD4')
   AND p.name IN ('PROCUREMENT_VIEW', 'PROCUREMENT_CAPTURE')
   AND NOT EXISTS (
       SELECT 1 FROM role_permissions rp
        WHERE rp.role_id = r.id AND rp.permission_id = p.id);
        </sql>
        <rollback>
            <sql>
DELETE FROM role_permissions
 WHERE role_id IN (SELECT id FROM roles WHERE name IN ('OFFICER','DD1','DD2','DD3','DD4'))
   AND permission_id IN (SELECT id FROM permissions
                          WHERE name IN ('PROCUREMENT_VIEW','PROCUREMENT_CAPTURE'));
            </sql>
        </rollback>
    </changeSet>

    <changeSet id="042-002" author="dms">
        <comment>Q-17/E-5: ADMIN keeps full procurement access explicitly</comment>
        <sql>
INSERT INTO role_permissions (role_id, permission_id)
SELECT r.id, p.id
  FROM roles r, permissions p
 WHERE r.name = 'ADMIN'
   AND p.name IN ('PROCUREMENT_VIEW', 'PROCUREMENT_CAPTURE', 'PROCUREMENT_VERIFY',
                  'PROCUREMENT_OVERRIDE', 'BUDGET_APPROVE')
   AND NOT EXISTS (
       SELECT 1 FROM role_permissions rp
        WHERE rp.role_id = r.id AND rp.permission_id = p.id);
        </sql>
        <rollback>
            <sql>
DELETE FROM role_permissions
 WHERE role_id = (SELECT id FROM roles WHERE name = 'ADMIN')
   AND permission_id IN (SELECT id FROM permissions
                          WHERE name IN ('PROCUREMENT_VIEW','PROCUREMENT_CAPTURE',
                                         'PROCUREMENT_VERIFY','PROCUREMENT_OVERRIDE',
                                         'BUDGET_APPROVE'));
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
